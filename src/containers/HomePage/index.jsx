import React from 'react';
import styled from 'styled-components';

const StyleWrapper = styled.div``;

export default class HomePage extends React.Component {
  render() {
    return <StyleWrapper>user home page</StyleWrapper>;
  }
}
