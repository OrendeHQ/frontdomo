import React from 'react';
import styled from 'styled-components';

const StyleWrapper = styled.div``;

export default class AdminHomePage extends React.Component {
  render() {
    return <StyleWrapper>admin home page</StyleWrapper>;
  }
}
